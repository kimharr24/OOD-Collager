package controller;

/**
 * Represents a text-based controller for the collage project. This controller expects the user
 * to enter text-based commands into a Readable.
 */
public interface TextController {
  /**
   * Executes the collage project.
   */
  void executeProgram();
}
